server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/snapfood_db
    username: postgres
    password: gol136802


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true